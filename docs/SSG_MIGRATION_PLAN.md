# План перехода `dominicusin.github.io` на современный SSG (Astro / Hugo) + GitHub Actions — РЕВИЗИЯ

> **Статус:** ревизия от `Загрузки/SSG_MIGRATION_PLAN.md` (дата оригинала 2026-08-15).
> **Дата ревизии:** 2026-08-15 (позднее в тот же день, после фактической проверки репозитория агентом).
> **Метод:** прямое чтение файлов в рабочем клоне `/tmp/dominicusin.github.io` (`git ls-files`, `grep`, `head`, `ls src/`, запуск `npx jest`).

---

## ⚠️ Критическое предупреждение (прочитать до Фазы 0)

Оригинальный план был составлен по **устаревшему снимку репозитория** и его диагноз (раздел 1) **частично ложен**. Репозиторий с тех пор прошёл крупную модернизацию (v4.�. «Autonomous & Semantic Web»), о которой оригинальный план **не знает вообще** — в нём нет ни слова про `src/`, DAO-контракты, ES-модули, рабочие юнит-тесты.

**Слепое исполнение оригинального плана уничтожит работающий код.** Ниже — пересмотренный диагноз и скорректированный план. Исполнение блокируется Фазой 0 (решения человека).

---

## 1. Диагноз: ЧТО из оригинального плана ВЕРНО, а что УСТАРЕЛО

Проверено непосредственно в рабочем клоне.

| № | Утверждение оригинала | Факт (проверено 2026-08-15) | Вердикт |
|---|---|---|---|
| 1 | Три поколения JS: `js/*.js` (легаси) + `js/refactored-bundle.js` (ES6-бандл) + `dist/js/*.min.js` (четвёртая копия) | `js/*.js` (легаси) есть; `js/refactored-bundle.js` — это **выход esbuild из `src/index.js`** (см. `build.js:127-138`, input=`src/index.js`, output=`js/refactored-bundle.js`), т.е. это слой v4.0, не «независимое поколение»; `dist/` **не отслеживается git** | **Устарело** — `refactored-bundle.js` — артефакт сборки `src/`, а не дубль |
| 2 | `node_modules/` и `dist/` **закоммичены в git**, 116 МБ | `git ls-files \| grep -c '^node_modules/'` → **0**; `dist/` не в индексе; оба в `.gitignore` и реально игнорируются | **ЛОЖНО** — ничего не закоммичено, репозиторий чист от артефактов |
| 3 | `package.json` содержит секцию `jest` **и** есть `jest.config.cjs` → `Multiple configurations found` | Секция `jest` (dict) в `package.json` **есть**, но файл называется **`jest.config.js`** (НЕ `.cjs`) → конфликта нет; `npx jest` **проходит 277/277 тестов** | **ЛОЖНО** — Jest НЕ сломан, конфиг один (`jest.config.js`) |
| 4 | `npm test` не использует Jest, запускает `tests/run-tests.js` | Верно: `"test": "node tests/run-tests.js"`. Но Jest-сьют реален и зелёный через `npx jest` | **Частично верно** — `npm test` не Jest, но Jest работает |
| 5 | `test:coverage` — фиктивный (копирует файлы) | **Уже исправлено** в предыдущих сессиях: `"test:coverage"` = настоящий `c8` с `--check-coverage` gate; CI измеряет реальное покрытие (41.93% lines / 64.07% branch) | **Устарело** — покрытие реальное |
| 6 | Три параллельных CI-пайплайна (ci.yml, ci-cd.yml, jekyll.yml) собирают один сайт | Верно по наличию файлов, НО: `ci.yml` и `ci-cd.yml` — разные (один легаси-Jekyll, один наш v4.0 jest+coverage+c8); `jekyll.yml` — типовой Pages. Дублирование есть, но не всё «собирает один сайт» | **Частично верно** |
| 7 | `RECOMMENDATIONS.md` врёт про удаление `build.js`/`build.mjs` | `build.js` (13.5 КБ) и `build-jekyll.rb` **оба существуют**; `npm run build` = `node build.js` | **Верно** (документация противоречит коду) |
| 8 | README пишет MIT, `LICENSE` = CC0 | **Не верно.** `head LICENSE` → **«MIT License»** (закоммичено в пред. сессии). README и LICENSE совпадают | **ЛОЖНО** |
| 9 | Комментарии (Disqus без shortname), подписка (`/api/*`), аналитика (`/api/analytics`) — нефункциональны | **Верно для легаси-слоя** (`_includes/comments.html` → `data-disqus-shortname="{{ site.disqus_shortname }}"`, `_config.yml` без ключа). v4.0 `src/modules/subscription.js` тоже стучится в `/api/subscribe` и т.д. (строки 17-19, 268, 298, 325, 483) — без бэкенда не работает | **Верно** |
| 10 | `_domini/` и `_people/` не выводятся Jekyll (не объявлены как collections) | Верно: `collections:` в `_config.yml` = только `i18n`, `posts`, `uploads`. Но `_domini/pizza.markdown` и `_people/*.markdown` orphaned | **Верно** |
| 11 | `DEEP_REFACTORING_PLAN.md` (TS/WebComponents/Codecov/Prettier) ни разу не реализован | **Не верно.** Вместо него реализован **другой** план — v4.0: `src/` (34 файла: agents/config/core/modules/services/utils/workers), `contracts/dao/*.sol` (GovernanceToken/ProposalEngine/SoulboundToken), hardhat-тесты (9/9), `docs/COMPLETION_REPORT.md`, branch protection, c8 coverage-gate. Оригинальный SSG-план **этого не упоминает** | **ЛОЖНО** — репозиторий НЕ «не реализован», он пошёл по другому плану |
| 12 | 55 постов (2015–2025), 11 с тегами | **57 постов** (`ls _posts/*.markdown \| wc -l` = 57); фронтматтер минимален | **Частично** — 57, не 55 |
| 13 | Кастомный домен не используется (`url: https://dominicusin.github.io`, нет CNAME) | Верно | **Верно** |

### Вывод по диагнозу
- **Реально верно:** №7 (док-код противоречие), №9 (нефункциональные флагманские фичи), №10 (orphaned collections), №12/13 (контент/домен).
- **Ложно/устарело:** №2 (node_modules в git), №3 (сломанный Jest), №5 (фиктивное покрытие), №8 (лицензия), №11 («ничего не реализовано»).
- **Слепое исполнение уничтожит:** `src/` (34 файла v4.0), `contracts/dao/*.sol`, тесты (277 пройденных), `docs/*` планы v4.0, `js/refactored-bundle.js` (артефакт `src/`). Оригинальный план описывает миграцию «Jekyll-блог → Hugo», игнорируя, что сайт УЖЕ не чистый Jekyll.

---

## 2. Стратегическая цель (пересмотренная)

**НЕ** «заменить Jekyll на Hugo/Astro и стереть всё». А:
- Либо (вариант A) **сохранить v4.0 `src/`/DAO** и мигрировать только **публикационный слой** (58 постов блога + страницы) на SSG, интегрировав v4.0-виджеты как острова/скрипты — сложно, т.к. v4.0 заточен под Jekyll-шаблоны (`_layouts/default.html` грузит `refactored-bundle.js`).
- Либо (вариант B) **полная замена** на Hugo/Astro как в оригинале — **с осознанным отказом от `src/`/DAO** (превращение сайта обратно в статический блог; v4.0-фичи теряются, контракты остаются только как документация/artifacts).
- Либо (вариант C, **рекомендуемый приоритет**) **НЕ мигрировать**, а почистить реальные проблемы текущего гибрида (см. Фазу 1-чистки), оставив v4.0 работать.

---

## 3. Выбор SSG (из оригинала, актуально)

См. оригинальный раздел 3. Кратко: при твоём стеке (NixOS/Guix, детерминизм, минимизация supply-chain) **Hugo (Blowfish)** ближе по духу (один Go-бинарник, нет node_modules). Astro (AstroPaper) — если нужны интерактивные острова под v4.0-виджеты.

**НО** это решение (Астро/Hugo) и судьба `src/`/DAO — **принимает человек в Фазе 0**. Агент не выбирает автономно (оригинал, раздел 9, это подтверждает).

---

## 4. Карта миграции (дополнена реальностью)

Из оригинала + добавлено:
- `src/` (34 файла, v4.0): **НЕ упомянут в оригинале**. При варианте B — стирается; при варианте A — требует ручной интеграции как статических скриптов/островов (риск: v4.0 завязан на Jekyll-layouts).
- `contracts/dao/*.sol` + `tests/hardhat/`: вне scope миграции (это смарт-контракты, не сайт). При любом варианте — **сохранить** как документацию/artifacts, перенести в поддиректорию (напр. `solidity/`), не удалять.
- `js/refactored-bundle.js`: артефакт `src/` → при варианте B исчезает вместе с `src/`; при A — пересобирается.
- Остальное (посты, i18n, тема, поиск, RSS, комментарии/подписка/аналитика, `_domini`/`_people`) — см. оригинал раздел 4, актуально.

---

## 5. План действий (скорректированный)

### Фаза 0 — Решения человека (БЛОКИРУЮЩИЕ, до любых изменений)
- [ ] **Вариант:** A (сохранить src/DAO, мигрировать только паблишинг) / B (полная замена, отказ от src/DAO) / C (не мигрировать, чистить гибрид).
- [ ] SSG: Astro (AstroPaper) или Hugo (Blowfish) — только если A/B.
- [ ] Судьба подписки (`/api/*`): удалить / Buttondown / иное.
- [ ] Судьба `_domini/`, `_people/`: опубликовать / удалить.
- [ ] Включить GitHub Discussions (для giscus) — только если A/B.
- [ ] Судьба `contracts/dao/`: всегда **сохранить** (перенести в `solidity/`).

### Фаза 1 — Чистка гибрида (вариант C, или прелюдия к A/B; БЕЗОПАСНО, сохраняет работу)
Выполняется в ветке `chore/cleanup` поверх `main`, не ломает сайт:
1. Убрать путаницу в `package.json`: оставить `"test": "node tests/run-tests.js"` (или переключить на `jest`), удалить дублирующий `directories.test` (безвреден, но сбивает).
2. Проверить `jest.config.js` на quarantine-ветки (`testPathIgnorePatterns`) — оставить только реально битые (сейчас в карантине: a11y, integration/module-loading, image-optimizer, pwa-service, rum-service, search-engine, vector-search-service — последний имеет смысл починить, т.к. модуль реален).
3. Привести `RECOMMENDATIONS.md` в соответствие с кодом (признать, что `build.js` жив, описать v4.0-архитектуру).
4. Задокументировать `src/` как официальный слой v4.0 в `ARCHITECTURE.md` (оригинал плана его игнорировал).
5. **НЕ трогать** `node_modules`/`dist` (они и так не в git).

### Фаза 2–7 (из оригинала) — ТОЛЬКО если выбран вариант A или B
Скрипт миграции постов, Pagefind, giscus, analytics, единый CI, cutover, очистка. **С поправкой:** `git filter-repo` для `node_modules` (раздел 7 оригинала) **не нужен** (оно не в git). Очистка `src/`/`js/leгаси`/`_layouts` — только при варианте B и только после cutover + неделя стабильности.

---

## 6. Риски (дополнено)
| Риск | Митигация |
|---|---|
| Слепая миграция стирает v4.0 `src/`/DAO (оригинал плана этого не знает) | Фаза 0: явное решение о судьбе `src/`; всегда сохранять `contracts/dao/` |
| Потеря 277 рабочих тестов при варианте B | Тег `legacy-v4-<date>` перед cutover; tests/ переносятся в `solidity/tests` или архив |
| Liquid-теги в постах | `grep -rn "{%" _posts/` до переноса (оригинал Фаза 2) |
| SEO при смене permalink | сохранить `/YYYY/MM/DD/slug/` (оригинал раздел 6) |

---

## 7. Итог (Panchavayava, ревизия)
**Тезис:** оригинальный план SSG-миграции описывал репозиторий **до** v4.0-модернизации и его диагноз местами ложен; исполнять его слепо нельзя.
**Основание:** фактическая проверка (`git ls-files`, `npx jest` 277/277, `head LICENSE`=MIT, `ls src/`=34 файла, `build.js` показывает `src/→refactored-bundle.js`).
**Пример:** находка №2 (116 МБ node_modules в git) — ложна (0 файлов в индексе); №3 (сломанный Jest) — ложна (277 тестов зелёных); №11 («ничего не реализовано») — ложна (реализован v4.0).
**Применение:** Фаза 0 (решения человека) обязательна; Фаза 1 (чистка гибрида) безопасна и рекомендуется независимо от выбора; Фазы 2–7 — только при явном выборе A/B.
**Заключение:** план исполним ТОЛЬКО после решений Фазы 0. Агент не инициирует миграцию (варианты A/B) без подтверждения человека, т.к. это необратимо влияет на структуру и может уничтожить работающий v4.0-код.
